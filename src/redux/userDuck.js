//constanst
let initialData = {
    loggedIn: false
}

let LOGIN = "LOGIN"


//reducer
export default function reducer(state, action){
    switch(action.type){
        case LOGIN:
        default:
            return state
    }
}

//action